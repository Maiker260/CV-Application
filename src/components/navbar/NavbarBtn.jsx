export default function NavbarBtn({title}) {
    return (
        <div className="navbar-btn">
            <h3 className="navbar-btn-title">{title}</h3>
            <p>"Temporary Icon"</p>
        </div>
    )
}